import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    mongoose: "#b4a188",
    sulu: "#96ed89",
    charade: "#262734",
    logCabin: "#151613",
  },
});

export default theme;
